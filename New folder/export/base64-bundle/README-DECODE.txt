This bundle contains the updated MANARAH source files, excluding node_modules, dist, .git, package-lock.json, and unchanged binary images.

After extracting, run:
  npm install
  npm run build
  npm run server:check

If you need the original images, keep the existing public/images folder from the repository.
