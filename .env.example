# PostgreSQL connection string for Manarah OS / Mishkat MVP
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/manarah?schema=public"

# API server settings
PORT=4000
CORS_ORIGIN="http://localhost:5173"

# Frontend API client
VITE_API_URL="http://localhost:4000"

# Temporary demo auth token used by the in-memory API prototype
DEMO_AUTH_TOKEN="demo-token"
DEMO_VIEWER_TOKEN="demo-viewer-token"

# Local JSON persistence for the API prototype
DATA_DIR="server/data"
